Left shift -acts as Exponent

Right Shift - Acts as divider

By Default we assume 32 bits.But its always dependents on the compiler

Unsigned = Greater than or Equal to Zero allowed.


0 to 2^32-1


Signed Integers- You Just use 31 bits to represent